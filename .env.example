# App server port
PORT=5000

# Optional GitHub personal access token to raise API rate limits
# Keep this secret and never commit your real token.
GITHUB_TOKEN=your_github_token_here

# Optional: cache duration (in seconds) for GitHub analysis responses
CACHE_TTL_SECONDS=300
