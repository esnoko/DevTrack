# App server port
PORT=5000

# Optional: GitHub Personal Access Token for higher rate limits (60 → 5000 req/hr)
# Create one at https://github.com/settings/tokens — no scopes required for public data
GITHUB_TOKEN=

# Optional: cache duration (in seconds) for GitHub analysis responses
CACHE_TTL_SECONDS=300
